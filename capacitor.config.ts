import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test-cap-issue-8367-regression-android-range',
  webDir: 'dist'
};

export default config;
